import { DiscoveryExplorer } from "../../components/discovery-explorer";

export default function FindLawyersPage() {
  return (
    <DiscoveryExplorer
      title="Search lawyers with filters that feel like real client intent, not a generic directory."
      description="Filter by practice area, city, court, language, and budget, then book a consultation with mocked payment and WhatsApp-ready lead capture."
      highlightLabel="Phase 2 discovery"
    />
  );
}

