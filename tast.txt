You are a senior frontend developer working with Next.js.

I have a web app deployed at https://nyayadwar.com. The app is triggering a browser permission popup saying:
"nyayadwar.com wants to access other apps and services on this device."

This likely comes from usage of navigator APIs such as navigator.share or similar device access APIs.

Tasks:

1. Identify any code that automatically triggers navigator.share or any device permission API on page load.
2. Refactor it so that:

   * These APIs are ONLY triggered after explicit user interaction (e.g., button click).
   * No permission prompts appear automatically when the page loads.
3. Add proper checks:

   * Use `if (navigator.share)` before calling it.
   * Wrap calls in try/catch.
4. Ensure graceful fallback if the API is not supported.

Additionally, there is another issue:
The UI shows: "Discovery cities could not be loaded."

Tasks:
5. Check API integration in Next.js:

* Ensure API base URL uses environment variable like:
  NEXT_PUBLIC_API_URL=https://api.nyayadwar.com

6. Verify frontend is calling correct endpoints.
7. Add error handling and loading states for failed API calls.
8. Log meaningful errors in console for debugging.

Goal:

* No permission popup on page load
* Clean user-triggered interactions
* API data loads correctly
* Production-ready frontend behavior
